name: 🚀 Deploy Seguro - ProCoder Tasks

on:
  push:
    branches:
      - main  # dispara o deploy quando fizer push na main

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout do código
        uses: actions/checkout@v3

      - name: 🔧 Instalar dependências
        run: npm ci

      - name: 🏗️ Build do projeto (Vite)
        run: npm run build

      - name: 🚀 Publicar build na branch gh-pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
          cname: ''  # deixe em branco ou defina domínio customizado
